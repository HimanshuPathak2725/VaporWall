[package]
name = "vaporwall"
version = "0.1.0"
edition = "2021"

[dependencies]
aya = "0.12"
tokio = { version = "1", features = ["full"] }
vaporwall-common = { path = "../vaporwall-common", features = ["user"] }
